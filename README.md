# Yuan Tools

This folder is the master workspace for Yuan's mini tools.

Each tool lives in its own `yuan-*` project folder and produces two release outputs:

- `release/html` for static web deployment
- `release/win` for the Windows desktop EXE

See `TOOLS.md` for the human-readable tool list and `tools.json` for the machine-readable registry.
